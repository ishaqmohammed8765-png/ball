{
  "name": "deterministic-balls-phaser",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "balance:harness": "node scripts/balance-harness.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^6.2.1",
    "vitest": "^3.2.4"
  }
}
